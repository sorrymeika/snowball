{
    "buildCommands": [
        "npm install",
        "ls -a"
    ],
    "autoConfig": [
        {
            "template": "package.json.vm",
            "destFile": "./package.json"
        }
    ]
}